'use strict';

// VW vehicles use the same device logic as Audi — the brand is stored in the
// device store and picked up automatically by AudiDevice.onInit().
module.exports = require('../audi/device');
